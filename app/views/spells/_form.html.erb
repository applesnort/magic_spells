<%= simple_form_for @spell do |f| %>
  <%= f.input :name %>
  <%= f.input :kind %>
  <%= f.input :level %>
  <%= f.input :photo, as: :file %>
  <%= f.submit "Add Spell", class: "btn btn-primary" %>
<% end %>
